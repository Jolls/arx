# v0.6 Major Feature Release — Suggested PR Plan

Remaining open work for the **v0.6 Major Feature Release** milestone, grouped into
suggested pull requests with dependency ordering. Completed items have been removed —
see git history for the full original plan with completed-item tracking.

> Status snapshot: 2026-07-11. Verified directly against `gh issue view`/`gh pr view`.
> #287 (DAT-2 CSV import) and #289 (AUD-1 audit/change log) have moved to the **v0.7 ECO
> and Audit Logs** milestone and are out of scope here. #658 (RPT-7) and #659 (RPT-8)
> shipped (PRs #661, #662); #660 (RPT-9, Low Stock Parts) has been unmilestoned from v0.6
> pending #273 and a re-scope. New issue #656 (Test Mode toggle UI) added to the v0.6
> milestone.

## Housekeeping / process (not PRs)

- ~~#431 — rotate DB password (release action, not a PR)~~ **done** (issue deleted from
  GitHub as part of scrubbing sensitive references before the repo went public)
- ~~#412 — unprivate repo (release/process action, not a PR)~~ **done** (PR #645/#646 —
  repo prepped for public viewing, AGPL-3.0 license added)

## Parts Master

### Purchase Orders

- ~~**#561** — Clean up legacy "None" PO line item. Spacer row from the Excel-era import,
  now has ~4993 links; needs a decision on delete-vs-leave before it's actionable, then a
  cleanup PR/data migration.~~ **done**

### Sourcing

- **#275** (SUP-1) — AVL attributes on sourcing links: preferred flag, approval status,
  MOQ, lead time, pack size on part-supplier links.

### Inventory (depends on PO receiving — done)

> Full architecture & stage breakdown: [inventory-management-system.md](inventory-management-system.md).
> Ledger-first design, built in stages: Stage 1 (core) and Stage 2 (receiving) are done.

- **#273** (INV-2) — Reorder points. Next unblocked inventory item.
- **#568** — Lot control on receiving (batch/vendor-lot ID captured at receipt). The
  Stage 4 lot/batch umbrella issue (#7) has been closed; #568 is the live remaining
  lot-control work.

### Reporting

- ~~**#282** (RPT-1) — Dashboard / home screen. Standalone.~~ **done** (PR #652 — Reports
  tab with KPI dashboard and per-user landing page)
- ~~**#283** (RPT-2) — Spend analysis.~~ **done** (PR #653 — Spend Analysis subtab, by
  supplier/by part, CSV export)
- ~~**#285** (RPT-4) — BOM cost summary.~~ **closed, not needed** — already satisfied by
  ENG-3's (#280) rollup-cost delta work: the BOM tab's tree table already shows unit/qty/
  extended cost per line, a rolled parent total, and a 5%-threshold delta flag vs. purchase
  price.
- ~~**#244** (RPT-5) — Pass/fail yield summary per form.~~ **done** (PR #655 — Yield
  Summary view per form, reachable from the records list and a new Reports subtab)
- ~~**#245** (RPT-6) — Failure mode report (top failing test steps per form).~~ **done**
  (PR #657 — Failure Modes report per form, plus Top Failing Steps and Lowest Yield Forms
  dashboard summary cards)
- ~~**#658** (RPT-7) — Additional dashboard summary cards: Stale WIP Records, POs Pending
  Approval. Low Stock Parts split out to #660 (RPT-9).~~ **done** (PR #661 — Stale WIP
  Records and POs Pending Approval dashboard cards)
- **#660** (RPT-9) — Low Stock Parts dashboard card, split out of #658. Blocked on INV-2
  (#273) min/max reorder fields — **unmilestoned from v0.6**; no longer tracked as
  in-scope here until #273 lands and it's re-scoped.
- ~~**#659** (RPT-8) — Supplier performance and data quality reports: Supplier On-Time
  Delivery, PO Cycle Time, Attachment/Data Quality Gaps (incl. no attachments on BUY/ASM/
  DWG-type parts).~~ **done** (PR #662)

### Audit & misc

- ~~**#463** — F1 post-migration follow-on. Historical username backfill is **done** (PR
  #633/#630); the issue stays open for the remaining, unresolved part: PO defaults
  (contact/receiver) — per-user vs. global. **done**~~
- **#540** — DB cleanup: drop columns no longer written by the app (rollup issue for
  non-backward-compatible drops — `contact.user_account_link`, `named_queries.active`
  rename, `release_status` CHECK constraint). Batch as one late-stage cleanup PR once all
  clients are on compatible versions.
- ~~**#537** — Consider a different color theme (current blue blends with header).
  Cosmetic/UX, standalone.~~ **done** (PR #650 — per-user accent color themes in Settings)
- **#651** — Dedicated Arx brand icon for the header home link (non-section-based).
  Follow-up to #282's landing-page link: the header brand mark currently reuses the
  per-section favicon and should be constant instead. Small standalone UX PR.
- ~~**#656** — Convert the Settings-page Test Mode control to a Bootstrap toggle switch and
  move it higher on the page (currently requires scrolling). Small standalone Settings/UX
  PR, no DB change.~~ **done** (PR #663)

## Test Records

### Audit & form management

- ~~**#508** — Allow creating a record against a prior locked/saved revision of the form.~~
  **closed, not needed** — revision visibility already shipped in #260 (Rev badge, Form Rev
  column); deliberately building against an old revision isn't a feature we need.
- ~~**#506** — Remove the one-time per-lock-history backfill bulk action (#251 follow-up).
  Cleanup task now that the backfill has run.~~ **done** (PR #647)

## Which PRs require a DB update on upgrade

`SQL/*.sql` files are **reference DDL, not migration runners** — they are not executed
automatically. Any PR that changes the schema (new table, new column, or a trigger
change) requires the admin to **manually run the DDL against the production DB *and* the
`ArxDev` test DB** before/after dropping in the new `Arx.exe`. PRs with no schema change
are pure binary swaps.

### Requires a DB migration

| PR | Issue | Change |
|---|---|---|
| ~~Legacy "None" PO line item cleanup~~ **done** | #561 | data migration, not a new column |
| AVL attributes | #275 | preferred flag, approval status, MOQ, lead time, pack size columns on part-supplier links |
| Reorder points | #273 | reorder threshold columns on `part`/inventory tables |
| Lot control on receiving | #568 | new lot/batch columns, possibly a lot table |
| DB cleanup — drop unused columns | #540 | column drops (non-backward-compatible by design) |

### No DB change — binary swap only

#651, #656. #660 has no DB change itself but is blocked on #273's migration landing first
and is no longer milestoned to v0.6. (~~#282~~, ~~#283~~, ~~#285~~, ~~#244~~, ~~#245~~,
~~#537~~, ~~#506~~, ~~#658~~, ~~#659~~ done.)

## Backward compatibility with 0.5.12

"Non-backward-compatible" here means: **after this PR's DB migration is applied, can the
old 0.5.12 `Arx.exe` still run against that database?** This matters for rollback safety
and any window where the old binary might still point at the migrated DB.

Verified fact that shapes this: the app's read queries use **explicit column lists** and
explicit positional `Scan` (not `SELECT *` into a flexible map). So adding a column does
**not** break the old binary's reads — it only selects the columns it knows about.

Rule of thumb for the remaining schema-touching PRs above: **add, never alter** (new
tables and additive columns), and **make every new column nullable or defaulted**. The one
known exception is **#540**, which is explicitly a non-backward-compatible cleanup PR —
batch it last, once all clients are confirmed upgraded.

## Summary

Remaining v0.6 scope: Inventory (reorder points #273, lot control #568), a standalone
cleanup PR (#540), a header-icon UX PR (#651), a Settings UX tweak (#656 — Test Mode
toggle), and AVL attributes (#275). #658 (RPT-7) and #659 (RPT-8) shipped in PRs #661 and
#662; #660 (RPT-9, Low Stock Parts, split out of #658) has been unmilestoned from v0.6
pending #273 (INV-2) landing and a re-scope. #282, #283, #244, and #245 shipped; #285 was
closed as already satisfied by #280's rollup-cost work. The other housekeeping/cleanup
items originally tracked here — #506, #537, #431, #412 — are all done, and #508 was
closed as not needed (the visibility piece already shipped in #260).

**Critical path:** Reorder points (#273) → lot control on receiving (#568) is the
remaining inventory sequence. Everything else in this list is independent and can be
picked up in any order.
