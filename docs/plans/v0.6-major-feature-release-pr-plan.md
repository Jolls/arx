# v0.6 Major Feature Release — Suggested PR Plan

Review of the **v0.6 Major Feature Release** milestone (66 open issues), grouped into
suggested pull requests with dependency ordering.

> Status snapshot: 2026-06-07. Issue numbers link to GitHub. This is a planning
> document — it does not change milestones or close issues on its own.

## Housekeeping first

**Exact duplicates** (pairs #256–259 vs #261–264 are the same four issues filed twice):

- #256 ↔ #261 — Required test steps
- #257 ↔ #262 — Conditional step visibility
- #258 ↔ #263 — Keyboard navigation
- #259 ↔ #264 — Auto-save / draft recovery

→ Close one of each pair before planning.

**Likely superseded by the structured epics:**

- #7 (Add Inventory Tracking) → umbrella for the INV-1/2/3 epic
- #9 (Add Leadtime) → covered by SUP-1 (#275) + SUP-3 (#277)
- #228 (ECO Process) → overlaps ENG-2 (#279)

**Arguably mis-milestoned** (hardening/test/release tasks, not features — consider moving
to `v0.4.1 Security & Hardening` or a release checklist):

- #431 (rotate DB password — a release action, not a PR)
- #361, #358, #369 (sev: med bugs)
- #374 (test coverage)

## Foundation PRs — do these first (everything else leans on them)

| PR | Issues | Why first |
|---|---|---|
| **F1 — User identity tracking** | #207 | Hard prerequisite for *every* audit/approval/sign-off feature below (AUD-1, PO-1, ENG-2, TR lock/reopen/reviewer, per-result history, form revisions). `SET CONTEXT_INFO` plumbing for triggers. |
| **F2 — `part_attribute` table** | #302, #281 (ENG-4) | ENG-4 custom field labels should land on the new typed table, not the EAV PNUser1-10 columns. Decide here. |
| **F3 — Typed config table** *(optional)* | #383 | Only if audit/UI config keys grow enough to justify it. Can defer. |

## Parts Master — feature epics

### Purchase Orders (sequence: lifecycle → approval → receiving → RFQ)

- PR: PO status lifecycle **#271** (PO-4) — the status field everything else keys off
- PR: PO approval workflow **#267** (PO-1) — needs F1 + #271
- PR: Receiving / goods receipt **#269** (PO-2) — feeds inventory
- PR: RFQ **#270** (PO-3)
- PR: Import files from part into PO folder **#156** (PO-adjacent, standalone)

### Inventory (depends on PO receiving #269)

- PR: Stock on hand **#272** (INV-1) — foundation
- PR: Min/max reorder points **#273** (INV-2) — needs INV-1
- PR: Inventory transactions log **#274** (INV-3) — needs INV-1; close #7 as covered

### Suppliers

- PR: AVL attributes on sourcing links **#275** (SUP-1)
- PR: Supplier performance metrics **#276** (SUP-2)
- PR: Lead time tracking per PO line **#277** (SUP-3) — folds in #9

### Engineering

- PR: Custom field labels **#281** (ENG-4) — bundle with F2
- PR: Alternate / substitute parts **#278** (ENG-1)
- PR: Part revision history / ECO log **#279** (ENG-2) — needs F1; closes #228
- PR: Cost roll-up UI **#280** (ENG-3) — pairs with RPT-4 below

### Reporting (after PO/inventory data exists)

- PR: Dashboard / home screen **#282** (RPT-1) + front-page readme **#347**
- PR: Spend analysis **#283** (RPT-2)
- PR: Price history chart **#284** (RPT-3)
- PR: BOM cost summary **#285** (RPT-4) — shares cost logic with ENG-3

### Data import/export

- PR: CSV/Excel export **#286** (DAT-1)
- PR: CSV import **#287** (DAT-2)
- PR: Barcode / QR labels **#288** (DAT-3)

### Audit & misc

- PR: Audit / change log **#289** (AUD-1) — needs F1
- PR: RoHS / compliance flags **#290** (AUD-2)
- PR: Create new PN with new base number **#346**

## Test Records — feature epics

### Record lifecycle (sequence: lock → reviewer/reopen → bulk/clone)

- PR: Lock/unlock from UI **#248** — foundation
- PR: Reviewer approval + reopen-with-reason **#249 + #250** — needs #248 + F1
- PR: Bulk lock **#253** — needs #248
- PR: Clone record **#254**

### Data quality / edit UX (after dedup)

- PR: Required steps + conditional visibility **#261 + #262** (close #256/#257)
- PR: Edit-view keyboard nav + auto-save **#263 + #264** (close #258/#259)

### Search & analytics

- PR: Global search + advanced filters **#246 + #247**
- PR: Yield summary + failure-mode report + yield dashboard **#244 + #245 + #252**
- PR: Export PDF + CSV **#242 + #243**

### Audit & form management (need F1)

- PR: Per-result change history **#251**
- PR: Form definition revision numbers **#260**
- PR: Definition history shows prior values **#389**

### Standalone TR

- PR: Hide/show columns **#386**
- PR: Back button with filter context **#390**
- PR: Reporting tab on forms **#388**
- PR: Row archive/retire **#403**

## Cross-app (after both apps' features land)

- PR: Cross-link Parts Master ↔ Test Records **#418** — already unblocked by v0.5 merge
- PR: Tooltips / intuitive help **#333** — touches both UIs; do late once features are stable

## Which PRs require a DB update on upgrade

`SQL/*.sql` files are **reference DDL, not migration runners** — they are not executed
automatically. So any PR that changes the schema (new table, new column, or a trigger
change) requires the admin to **manually run the DDL against the production DB *and* the
`ArxDev` test DB** before/after dropping in the new `Arx.exe`. PRs with no schema change
are pure binary swaps.

These are design-level estimates — the issues aren't built yet, so a few depend on final
approach (flagged ⚠️).

### Requires a DB migration

New tables:

| PR | Issue | Change |
|---|---|---|
| F2 — part_attribute | #302 / #281 | new `part_attribute` table |
| F3 — typed config | #383 | new config table + migrate `app_config` |
| PO receiving | #269 | new goods-receipt table |
| PO RFQ | #270 | new RFQ table(s) |
| Inventory transactions log | #274 | new transactions table |
| Alternate/substitute parts | #278 | new substitutes table |
| Part revision / ECO log | #279 | new revision table |
| Audit / change log | #289 | new `change_log` table |
| TR per-result change history | #251 | new result-audit table |

New columns on existing tables:

| PR | Issue | Change |
|---|---|---|
| PO status lifecycle | #271 | status column on PO |
| PO approval workflow | #267 | approval status/approver columns on PO |
| Stock on hand | #272 | stock qty field/table on part |
| Min/max reorder points | #273 | reorder columns |
| AVL attributes on LNK | #275 | preferred/MOQ/lead-time/etc. on `LNK` |
| Lead time per PO line | #277 | lead-time column on PO line |
| RoHS / compliance flags | #290 | compliance columns on `PN` |
| TR reviewer approval + reopen | #249 / #250 | reviewer/status/reason columns |
| TR required steps + conditional | #261 / #262 | required-flag + condition columns on `test_definition` |
| TR form revision numbers | #260 | revision column on form definition |
| TR row archive/retire | #403 | archived flag (replaces HIDE workaround) |

Trigger changes (also a manual DB step, easy to overlook):

| PR | Issue | Change |
|---|---|---|
| F1 — user identity | #207 | `test_definition_history` + `TestRecordHistory` triggers use app user via `CONTEXT_INFO` instead of `SYSTEM_USER` |
| TR definition history shows prior values | #389 | trigger rewritten to capture old values |

### Probably requires DB change — depends on final design ⚠️

- **TR auto-save / draft recovery** (#264) — likely needs a draft-storage table or column
- **ENG-4 custom field labels** (#281) — no change if labels stay in `app_config`; DB change if it rides on F2's `part_attribute`
- **TR hide/show columns** (#386) — depends on whether per-user prefs persist to DB
- **Supplier performance metrics** (#276), **ENG-3 cost roll-up** (#280), **RPT-3 price history** (#284) — read-only *if* the source data is already stored; otherwise may add capture columns

### No DB change — binary swap only

#156, #346, #253, #254, #248, #263, #246, #247, #242, #243, #244, #245, #252, #282, #283,
#285, #286, #287, #288, #388, #390, #418, #333.

### Operational caveat

There is currently no migration runner, so every schema PR adds manual DDL the admin must
apply in the right order (and to both prod + ArxDev). With ~25 schema-touching PRs in v0.6,
that's a lot of hand-run scripts — a lightweight ordered-migration mechanism may be worth
its own issue before the epic ramps up.

## Summary

~40 PRs for 66 feature issues — the structured `[XXX-N]` epics map almost 1:1 to PRs.

**Critical path:**

1. **F1 (user identity)** → unblocks all audit/approval/sign-off features
2. **PO lifecycle (#271) → receiving (#269) → inventory (INV-1/2/3)**

Tackle F1 + the PO epic first since they unblock the widest set of downstream work.
